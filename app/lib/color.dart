/*
 * File: color.dart
 * Description: Defines color constants for the Edudoro application's design system.
 * Responsibilities:
 * - Provides primary, secondary, and semantic color values.
 * - Centralizes color definitions for consistent theming.
 * Author: Auttakorn Camsoi
 * Course: Mobile Application Development Framework
 */

import 'package:flutter/material.dart';

/// The primary brand color.
const primary = Color.fromARGB(255, 130, 0, 0);

/// The secondary accent color.
const secondary = Color.fromARGB(255, 255, 211, 211);

/// The background color for major surfaces.
const background = Color.fromARGB(255, 255, 104, 104);

/// Highlight color for positive actions or currency.
const yellow100 = Color.fromARGB(255, 255, 230, 0);

/// Highlight color for success or confirmation.
const green100 = Color.fromARGB(255, 17, 255, 0);

/// Color for destructive or error actions.
const destructive = Color.fromARGB(255, 255, 0, 0);

/// Standard white color for text or backgrounds.
const white = Colors.white;
